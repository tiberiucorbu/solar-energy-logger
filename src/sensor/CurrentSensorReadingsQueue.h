#ifndef _CURRENT_SENSOR_READINGS_QUEUE_
#define _CURRENT_SENSOR_READINGS_QUEUE_

  // #include "CurrentSensorReadings.h"

  // static QueueHandle_t currentSensorReadingsQueue = xQueueCreate(10, sizeof(CurrentSensorReadings));

#endif
